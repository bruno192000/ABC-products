import React from "react";
import { useQuery } from '@apollo/client';

const Home = () => {
    return (
        <div className="container">
            <p>This is a test.</p>
        </div>
    );
};

export default Home;